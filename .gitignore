# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies (directory or local worktree symlink)
node_modules

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android

# Local env (never commit real keys)
.env
.env.local

# VAPID keypair for local push testing — never commit.
.vapid.local.json

# Supabase CLI local state
supabase/.temp/

# Vercel CLI project link
.vercel/

# Local-only secrets handed to the Supabase CLI — never commit.
.secrets.local
