/**
 * Native score entry already has real haptic feedback. The audio detent is a
 * web fallback, so these functions intentionally do nothing on native.
 */
export function prepareScoreDialAudio(): void {}

export function playScoreDialClick(): void {}
